name = "timevault"
compatibility_date = "2026-06-21"
pages_build_output_dir = "dist"

# KV 命名空间绑定 - 用于存储统计数据和用户留言
# 创建 KV 后，将下面的 id 替换为你的实际 Namespace ID
[[kv_namespaces]]
binding = "STATS"
id = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"  # 替换为你复制的 Namespace ID
